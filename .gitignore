# Python packaging artifacts
*.egg-info/
build/
dist/
__pycache__/
*.pyc

# Local dev server logs
streamlit_log.txt

# Pre-sync safety copies (source + old model binaries) — never deploy these
_prefix_backup_*/